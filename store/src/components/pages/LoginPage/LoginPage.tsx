import { FC } from 'react';
import LoginForm from '../../forms/LoginForm/LoginForm';

const LoginPage: FC = () => {
  return (
    <div>
      <h3>login</h3>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
